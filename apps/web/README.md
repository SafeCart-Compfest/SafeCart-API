# SafeCart web

The mobile-first PWA starts only after the AI acceptance gates in
`docs/EXPERIMENT_PLAN.md` pass. Keeping this directory intentionally empty prevents UI
work from hiding model and data risks during the AI-first phase.

