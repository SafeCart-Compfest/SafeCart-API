# Artifact manifests

Model weights are stored outside Git. Commit only immutable artifact metadata containing
the model version, source experiment, download URL, size, format, runtime requirements,
and SHA-256 checksum.

